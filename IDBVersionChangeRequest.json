{
  "name": "IDBVersionChangeRequest",
  "description": "Warning: The latest specification does not include this interface anymore as the IDBDatabase.setVersion() method has been removed. See the compatibility table for version details. The new way to do it is to use the IDBOpenDBRequest interface which has now the onblocked handler and the newly needed onupgradeneeded one. The IDBVersionChangeRequest interface the IndexedDB API represents a request to change the version of a database. It is used only by the setVersion() method of IDBDatabase.",
  "members": [
    {
      "name": "IDBVersionChangeRequest.setVersion",
      "link": "/en-US/docs/Web/API/IDBVersionChangeRequest/setVersion",
      "description": "Updates the version of the database. Returns immediately and runs a versionchange transaction on the connected database in a separate thread.",
      "parameters": [
        {
          "name": "version",
          "description": "The version to store in the database."
        },
        {
          "name": "IDBVersionChangeRequest",
          "description": "The request to change the version of a database."
        }
      ]
    }
  ]
}