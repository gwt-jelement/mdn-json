{
  "name": "IDBFactory",
  "description": "",
  "members": [
    {
      "name": "IDBFactory.open",
      "link": "/en-US/docs/Web/API/IDBFactory/open",
      "description": "The current method to request opening a connection to a database.",
      "parameters": [
        {
          "name": "name",
          "description": "The name of the database."
        },
        {
          "name": "version Optional",
          "description": "Optional. The version to open the database with. If the version is not provided and the database exists, then a connection to the database will be opened without changing its version. If the version is not provided and the database does not exist, then it will be created with version 1."
        },
        {
          "name": "options (version and storage) Optional   ",
          "description": "Optional. In Gecko, since version 26, you can include a non-standard options object as a parameter of IDBFactory.open that contains the version number of the database, plus a storage value that specifies whether you want to use persistent or temporary storage."
        }
      ]
    },
    {
      "name": "IDBFactory.deleteDatabase",
      "link": "/en-US/docs/Web/API/IDBFactory/deleteDatabase",
      "description": "A method to request the deletion of a database.",
      "parameters": [
        {
          "name": "name",
          "description": "The name of the database you want to delete. Note that attempting to delete a database that doesn\u0027t exist does not throw an exception, in contrast to IDBDatabase.deleteObjectStore(), which does throw an exception if the named object store does not exist."
        },
        {
          "name": "options Non-standard",
          "description": "In Gecko, since version 26, you can include a non-standard optional storage parameter that specifies whether you want to delete a permanent (the default value) IndexedDB, or an indexedDB in temporary storage (aka shared pool.)"
        }
      ]
    },
    {
      "name": "IDBFactory.cmp",
      "link": "/en-US/docs/Web/API/IDBFactory/cmp",
      "description": "A method that compares two keys and returns a result indicating which one is greater in value.",
      "parameters": [
        {
          "name": "first",
          "description": "The first key to compare."
        },
        {
          "name": "second",
          "description": "The second key to compare."
        }
      ]
    },
    {
      "name": "IDBFactory.open",
      "link": "/en-US/docs/Web/API/IDBFactory.open-obsolete",
      "description": "An obsolete method to request opening a connection to a database, still implemented by some browsers.",
      "parameters": [
        {
          "name": "name",
          "description": "The name of the database."
        }
      ]
    }
  ]
}